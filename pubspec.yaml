name: whiskr_admin_panel
description: "Whiskr Pet Shop Admin Panel - A comprehensive admin interface for managing pet shop operations."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1


dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  
  # HTTP and API communication
  http: ^1.1.0
  dio: ^5.3.2
  provider: ^6.1.1  
  flutter_dotenv: ^5.1.0
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.0
  image_picker: ^1.0.4
  shared_preferences: ^2.2.2
  form_validator: ^2.1.1
  flutter_spinkit: ^5.2.0
  file_picker: ^8.0.0+1  # Update to latest version
  qr_flutter: ^4.1.0
  responsive_framework: ^1.5.1
  go_router: ^16.2.4

  # network module
  w_network_module:
    git:
      url: https://github.com/whiskr-pet/whiskr_modules.git
      path: packages/core/w_network_module
      ref: 56de089cbc4ca92ccf42a989968e3de39295fd3e
  w_utils:
    git:
      url: https://github.com/whiskr-pet/whiskr_modules.git
      path: packages/core/w_utils
      ref: 56de089cbc4ca92ccf42a989968e3de39295fd3e
  w_components:
    git:
      url: https://github.com/whiskr-pet/whiskr_modules.git
      path: packages/core/w_components
      ref: 56de089cbc4ca92ccf42a989968e3de39295fd3e  
  w_authentication:
    git:
      url: https://github.com/whiskr-pet/whiskr_modules.git
      path: packages/core/w_authentication
      ref: 56de089cbc4ca92ccf42a989968e3de39295fd3e   
  w_permissions_module:
    git:
      url: https://github.com/whiskr-pet/whiskr_modules.git
      path: packages/mobile/w_permissions_module
      ref: 56de089cbc4ca92ccf42a989968e3de39295fd3e
  data_table_2: ^2.6.0
  flutter_side_menu: ^0.5.41



dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: NunitoSans
      fonts:
        - asset: assets/fonts/NunitoSans-VariableFont_YTLC,opsz,wdth,wght.ttf
        - asset: assets/fonts/NunitoSans-Italic-VariableFont_YTLC,opsz,wdth,wght.ttf
          style: italic
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
